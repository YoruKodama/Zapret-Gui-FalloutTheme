# 🚀 Инструкция по сборке Zapret Matrix GUI в .exe

## 📋 Требования для сборки

- **Python 3.8+** (только для сборки, не нужен пользователям!)
- **Интернет** (для установки PyInstaller)
- **Около 300 MB свободного места**

## ⚡ Быстрый старт

### Вариант 1: Автоматическая сборка (рекомендуется)

Просто запустите:
```batch
build_exe.bat
```

Скрипт автоматически:
- ✅ Проверит наличие Python
- ✅ Установит PyInstaller (если нужно)
- ✅ Установит зависимости
- ✅ Соберет .exe файл

### Вариант 2: Ручная сборка

```batch
# 1. Установите зависимости
pip install -r requirements_gui.txt
pip install pyinstaller

# 2. Запустите скрипт сборки
python build_exe.py
```

## 📦 Результат сборки

После успешной сборки вы получите:

```
dist/
  └── ZapretMatrixGUI.exe  ← Готовый исполняемый файл
```

**Размер файла:** ~50-80 MB (все библиотеки упакованы внутри)

## 🎁 Распространение

Для распространения готового приложения создайте архив со следующей структурой:

```
ZapretMatrixGUI/
  ├── ZapretMatrixGUI.exe          ← Главный файл из dist/
  ├── zapret_matrix_config.json    ← Конфигурация
  ├── bin/                          ← Binaries zapret
  │   ├── winws.exe
  │   └── WinDivert64.sys
  ├── lists/                        ← Списки доменов
  │   ├── list-general.txt
  │   ├── list-google.txt
  │   └── ...
  └── README.md                     ← Инструкция для пользователей
```

## ✅ Преимущества

- ✨ **Пользователям НЕ нужен Python!**
- ✨ **Один файл** - легко распространять
- ✨ **Все зависимости внутри** - никаких pip install
- ✨ **Двойной клик** - и программа запускается
- ✨ **Работает на чистой Windows** без предустановок

## 🔧 Настройка сборки

Если хотите изменить параметры сборки, отредактируйте [build_exe.py](build_exe.py):

```python
pyinstaller_args = [
    'zapret_matrix_gui.py',
    '--name=ZapretMatrixGUI',      # Имя файла
    '--onefile',                     # Один файл
    '--windowed',                    # Без консоли
    '--icon=icon.ico',               # Добавьте свою иконку
    # ... остальные параметры
]
```

### Полезные опции:

- `--icon=path/to/icon.ico` - добавить иконку
- `--version-file=version.txt` - добавить версию
- `--uac-admin` - запрашивать права администратора
- `--console` - показывать консоль (для отладки)

## ⚠️ Возможные проблемы

### "PyInstaller не найден"
```batch
pip install pyinstaller
```

### "ModuleNotFoundError при запуске .exe"
Добавьте модуль в `--hidden-import`:
```python
'--hidden-import=название_модуля',
```

### "Файл слишком большой"
Используйте `--onedir` вместо `--onefile` для создания папки вместо одного файла:
```python
'--onedir',  # Вместо --onefile
```

### Антивирус блокирует .exe
Это нормально для упакованных PyInstaller файлов. Добавьте в исключения или:
1. Используйте code signing (подпись кода)
2. Загрузите на VirusTotal для проверки
3. Распространяйте через официальные каналы

## 🧪 Тестирование

После сборки протестируйте .exe файл:

### Быстрая проверка

```batch
# Проверить наличие и размер
dir dist\ZapretMatrixGUI.exe
```

### Полное тестирование

1. **Создайте тестовую папку**
```batch
mkdir test_portable
copy dist\ZapretMatrixGUI.exe test_portable\
copy zapret_matrix_config.json test_portable\
xcopy bin test_portable\bin\ /E /I
xcopy lists test_portable\lists\ /E /I
```

2. **Запустите и проверьте:**
   - ✅ Окно открылось
   - ✅ Интерфейс отображается корректно
   - ✅ Выбор стратегии работает
   - ✅ Активация запускает сервис
   - ✅ Деактивация останавливает сервис
   - ✅ Логи выводятся

3. **Тест на чистой системе (опционально):**
   - Создайте виртуальную машину с Windows
   - Скопируйте папку test_portable
   - Запустите без установки Python
   - Проверьте все функции

## ⚠️ Возможные проблемы

### "PyInstaller не найден"
```batch
pip install pyinstaller
```

### "ModuleNotFoundError при запуске .exe"
Добавьте модуль в `build_exe.py`:
```python
'--hidden-import=название_модуля',
```

### "Антивирус блокирует .exe"
Это нормально для PyInstaller файлов:
- Добавьте в исключения антивируса
- Проверьте на VirusTotal
- Используйте code signing (для профессионального распространения)

## � Создание дистрибутива

Автоматически создайте готовый пакет для распространения:

```batch
create_distribution.bat
```

Это создаст папку `ZapretMatrixGUI_Portable/` со всеми необходимыми файлами:
- ZapretMatrixGUI.exe
- zapret_matrix_config.json
- bin/ (winws.exe, WinDivert64.sys)
- lists/ (списки доменов)
- README.md (инструкция для пользователей)

## 📊 Параметры сборки

### Изменить настройки

Отредактируйте `build_exe.py`:

```python
pyinstaller_args = [
    'zapret_matrix_gui.py',
    '--name=ZapretMatrixGUI',      # Имя файла
    '--onefile',                     # Один файл
    '--windowed',                    # Без консоли
    '--icon=icon.ico',               # Добавьте иконку (если есть)
    # ... остальные параметры
]
```

### Полезные опции

- `--icon=path/to/icon.ico` - добавить иконку
- `--console` - показывать консоль (для отладки)
- `--onedir` - создать папку вместо одного файла (меньше размер)

---

**Готово!** 🎉 Теперь ваши пользователи могут запускать Zapret Matrix GUI без установки Python!
